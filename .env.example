OPENROUTER_API_KEY=your_openrouter_key_here

# Step 1: copy the example to create your real .env
# cp .env.example .env

# Step 2: open .env in any text editor and replace the placeholder
# Notepad, VS Code, nano — anything works
